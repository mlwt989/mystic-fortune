# ===== DeepSeek API Key =====
# 去 https://platform.deepseek.com 注册，在「API Keys」页面创建
# 把下面的值换成你自己的 key（以 sk- 开头）
DEEPSEEK_API_KEY=sk-your-key-here

# 可选：覆盖模型（默认 deepseek-chat，便宜好用）
# 也可换成 deepseek-reasoner（推理模型，更慢更贵）
# DEEPSEEK_MODEL=deepseek-chat

# 端口由部署平台自动设置，本地跑默认 3000，一般不用改
# PORT=3000
